spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://localhost:9099
      fail-fast: true
      password: ${CONFIGURATION_SERVER_PASSWORD}
      username: ${CONFIGURATION_SERVER_USER}
